/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    domains: ['1213f06464.nxcli.net', 'images.pexels.com'],
  },
}

module.exports = nextConfig
